# TODOs

| ID | Status | Bereich | Aufgabe | Notizen |
|------|--------|---------|---------|---------|
| T-001 | Offen | CSS / Design | Schriftgrößen per CSS-Variablen definieren und einheitlich nutzen | Derzeit viele verstreute `font-size`-Angaben in `common.css`, `dashboard.css` und `admin.css` (ca. 40+). Skala in `:root` anlegen (z. B. `--text-xs`, `--text-sm`, `--text-base`, `--text-lg`, `--text-xl`) und Ad-hoc-Werte (`0.7rem`, `0.875rem`, `14px`, `0.95rem`, …) ersetzen. |
| T-002 | Offen | CSS / Design | `font`-Shorthand prüfen (`font: inherit` usw.) | Kurzschreibweise darf keine expliziten Größen überschreiben (vgl. Select-Fix in `common.css`). |
| T-003 | Erledigt | Dashboard / Charts | Annotations zu Zeitpunkten (Deploys, Änderungen) | **Umgesetzt:** projektweite Annotations mit freiem `annotated_at` (Datum + Uhrzeit), `label` und optionalem `link`. **Backend:** Tabelle `annotations` + CRUD-API (`/api/projects/:id/annotations`, Berechtigung wie Projekte) + Share-Read (`/api/public/share/:id/annotations`, ohne `created_by`); **PATCH** zum Bearbeiten. **Frontend:** Chart.js-Plugin (Marker, Tooltip, Klick → Liste), Formular + Liste mit Inline-Bearbeiten (Share schreibgeschützt). **Optional später:** Bindung an konkreten `run_id`. |
| T-004 | Erledigt | Admin / GitHub | Fork-Sync per Button (private Instanz) | Admin → **Upstream sync**: Status (ahead/behind) und Button **Sync from upstream**; GitHub API (`merge-upstream` für Forks, cross-repo merge für Template-Kopien). `GH_PAT` mit Contents Read+Write; Upstream owner/repo/branch in Instance settings (Defaults: `platomat` / `page-speed-tester-demo` / `main`). |
| T-005 | Erledigt | Betrieb / Recovery | Passwort-Reset über D1 (SQLite) | `npm run hash-password` / `scripts/hash-password.mjs` (lokal, Shared-Modul `shared/password-hash.mjs`); D1-`UPDATE` dokumentiert in `INSTALLATION.md`. Worker: Bootstrap, Login, User-Anlage nutzen dasselbe Modul. |
| T-006 | Erledigt | DB / Deploy | Schema-Version + automatische Migrationen | **Umgesetzt** mit Wranglers eingebautem D1-Migrationssystem (Tracking-Tabelle `d1_migrations` = DB-Version): nummerierte Dateien in `migrations/` (`0001_baseline.sql` …), nur offene werden ausgeführt. `npm run db:migrate` (lokal) / `db:migrate:remote` (remote) / `db:migrate:new` (anlegen) / `db:migrate:list` (Status). `npm run deploy` und das Git-Deploy-Build-Kommando wenden Remote-Migrationen **vor** dem Deploy an. `schema.sql` entfernt → Single Source of Truth `migrations/`. Unterstützt beliebige SQL (Spalten umbenennen, Daten kopieren). Doku: `migrations/README.md`. |
| T-007 | Erledigt | Auth / Security | Login Rate-Limit mit progressivem Timeout | **Umgesetzt:** KV-basiertes Rate-Limit in `worker/src/login-rate-limit.ts` — fehlgeschlagene Versuche pro **IP** und **Login-Identifier** (normalisiert), exponentielles Backoff (1s → 2s → 4s …, Deckel 15 min). Vor dem Login geprüft → **429** mit `retry_after_seconds`; nach Erfolg Zähler gelöscht. Timing-sicherer Dummy-Hash wenn User fehlt (keine Enumeration). Login-UI zeigt Restwartezeit und deaktiviert Submit-Button. |
| T-008 | Erledigt | Projekte / Reports | Screenshot-Einstellungen pro Projekt + Umbenennungen | **Umgesetzt:** `store_fullpage_screenshots` und `store_timing_screenshots` als Spalten auf `projects` (Migration `0005_project_screenshot_settings.sql`; globale Settings migriert und entfernt). Admin: Checkboxen in Projekt-Tabelle und Create-Formular; Instance settings ohne Screenshot-Flags. Dispatch/CI: `store_fullpage_screenshots` → `STORE_FULLPAGE_SCREENSHOTS`. `runs.has_fullpage_screenshots` war bereits in `0004`. |
| T-009 | Offen | Dashboard / Projekte | CRUD-Routen und -Seiten für Projekte in der UI | **Ist:** Projekt-Verwaltung (Anlegen, Bearbeiten, Löschen, URLs, Cron) liegt monolithisch in `admin.html` / `admin.js` als Tabellen-Inline-Editing. **Ziel:** Eigene UI-Routen/Seiten für Projekte — z. B. Liste, Detail/Edit, Create — statt alles auf der Admin-Seite. **Backend:** CRUD-API existiert bereits (`/api/projects`, URLs, Berechtigungen). **Frontend:** Routing/Navigation (Header, Breadcrumbs), Formulare und Listen aus `admin.js` auslagern; Admin ggf. nur noch für User/Instance-Settings. **Optional:** URL-Verwaltung pro Projekt als Unterseite oder Tab. |
| T-010 | Erledigt | Projekte / CI | `LH_WARMUP` pro Projekt konfigurierbar | **Umgesetzt:** Spalte `lh_warmup` auf `projects` (Migration `0006_project_lh_warmup.sql`, Default aus). Admin: **W**-Checkbox in Projekt-Tabelle/Create-Formular. Dispatch → `LH_WARMUP` in CI; Warmup mit `sleep-jitter` (`LH_WARMUP_DELAY_SEC`, Default 2s ± Jitter) vor dem `curl`. |

**Status-Legende:** `Offen` · `In Arbeit` · `Erledigt`
