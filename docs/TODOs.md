# TODOs

| ID | Status | Bereich | Aufgabe | Notizen |
|------|--------|---------|---------|---------|
| T-001 | Offen | CSS / Design | Schriftgrößen per CSS-Variablen definieren und einheitlich nutzen | Derzeit viele verstreute `font-size`-Angaben in `common.css`, `dashboard.css` und `admin.css` (ca. 40+). Skala in `:root` anlegen (z. B. `--text-xs`, `--text-sm`, `--text-base`, `--text-lg`, `--text-xl`) und Ad-hoc-Werte (`0.7rem`, `0.875rem`, `14px`, `0.95rem`, …) ersetzen. |
| T-002 | Offen | CSS / Design | `font`-Shorthand prüfen (`font: inherit` usw.) | Kurzschreibweise darf keine expliziten Größen überschreiben (vgl. Select-Fix in `common.css`). |
| T-003 | Erledigt | Dashboard / Charts | Annotations zu Zeitpunkten (Deploys, Änderungen) | **Umgesetzt:** projektweite Annotations mit freiem `annotated_at` (Datum + Uhrzeit), `label` und optionalem `link`. **Backend:** Tabelle `annotations` + CRUD-API (`/api/projects/:id/annotations`, Berechtigung wie Projekte) + Share-Read (`/api/public/share/:id/annotations`, ohne `created_by`). **Frontend:** Chart.js-Plugin zeichnet vertikale Marker (zeitlich interpoliert zwischen Runs) auf alle Charts; Hover-Tooltip mit Datum/Label/Link; Formular + Liste im Dashboard (Share schreibgeschützt). `dashboard/assets/js/app.js`, `worker/src/annotations.ts`. **Optional später:** Bindung an konkreten `run_id`, Inline-Bearbeiten statt Löschen+Neu. |
| T-004 | Erledigt | Admin / GitHub | Fork-Sync per Button (private Instanz) | Admin → **Upstream sync**: Status (ahead/behind) und Button **Sync from upstream**; GitHub API (`merge-upstream` für Forks, cross-repo merge für Template-Kopien). `GH_PAT` mit Contents Read+Write; Upstream owner/repo/branch in Instance settings (Defaults: `platomat` / `page-speed-tester-demo` / `main`). |
| T-005 | Erledigt | Betrieb / Recovery | Passwort-Reset über D1 (SQLite) | `npm run hash-password` / `scripts/hash-password.mjs` (lokal, Shared-Modul `shared/password-hash.mjs`); D1-`UPDATE` dokumentiert in `INSTALLATION.md`. Worker: Bootstrap, Login, User-Anlage nutzen dasselbe Modul. |
| T-006 | Offen | DB / Deploy | Schema-Version + automatische Migrationen | **`schema_version`** (oder Meta-Tabelle) in D1; nummerierte Migrationsdateien (`migrations/001_….sql`, …). Beim **Build/Deploy** (oder `npm run db:migrate`): aktuelle Version lesen, fehlende Scripts der Reihe nach ausführen, Version hochzählen. Ersetzt manuelles SQL-Pflegen für dich und Kunden; `schema.sql` ggf. nur noch Baseline für frische Instanzen. Abstimmung mit bestehenden `db:migrate` / `db:migrate:remote` Scripts. |
| T-007 | Offen | Auth / Security | Login Rate-Limit mit progressivem Timeout | Brute-Force-Schutz beim Login: fehlgeschlagene Versuche pro Konto/IP zählen (KV) und bei Falscheingabe eine **immer länger werdende Wartezeit** erzwingen (exponentielles Backoff, z. B. 1s → 2s → 4s → … bis Deckel). Nach erfolgreichem Login Zähler zurücksetzen. Offene Punkte: Schlüssel (IP vs. Login-Identifier vs. beides), Speicherung/TTL in KV, klare Fehlermeldung mit Restwartezeit im UI, Schutz gegen Account-Enumeration (gleiche Antwort/Timing). |

**Status-Legende:** `Offen` · `In Arbeit` · `Erledigt`
