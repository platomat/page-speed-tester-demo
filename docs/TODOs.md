# TODOs

| Status | Bereich | Aufgabe | Notizen |
|--------|---------|---------|---------|
| Offen | CSS / Design | Schriftgrößen per CSS-Variablen definieren und einheitlich nutzen | Derzeit viele verstreute `font-size`-Angaben in `common.css`, `dashboard.css` und `admin.css` (ca. 40+). Skala in `:root` anlegen (z. B. `--text-xs`, `--text-sm`, `--text-base`, `--text-lg`, `--text-xl`) und Ad-hoc-Werte (`0.7rem`, `0.875rem`, `14px`, `0.95rem`, …) ersetzen. |
| Offen | CSS / Design | `font`-Shorthand prüfen (`font: inherit` usw.) | Kurzschreibweise darf keine expliziten Größen überschreiben (vgl. Select-Fix in `common.css`). |
| Offen | Dashboard / Charts | Annotations zu Zeitpunkten (Deploys, Änderungen) | **Ziel:** Festhalten, was an der Website geändert wurde, und im Chart sehen, ob sich die Testwerte danach verschieben. **UI:** Annotation anlegen (Text, optional Link); im Chart als Marker/Punkt auf der Zeitachse; bei Hover Tooltip mit Text (ggf. Autor/Datum). **Offene Entscheidung:** An Annotation an einen konkreten **Run** binden (exakter Messzeitpunkt, eindeutig) oder **projektweit** mit freiem **Datum + Uhrzeit** (flexibler für „Deploy um 14:30“, auch ohne Run in dem Slot). Hybrid denkbar: primär projektbezogen mit `annotated_at`, optional `run_id` wenn vorhanden. **Backend:** neue Tabelle/API (CRUD, Berechtigung wie Projekte); **Frontend:** Chart.js-Plugin oder vertikale Linie + Tooltip; Admin/Dashboard-Formular zum Anlegen/Bearbeiten. |
| Offen | Admin / GitHub | Fork-Sync per Button (private Instanz) | Im Admin-Bereich der **privaten** Repo-Instanz: Button „Fork synchronisieren“ (Upstream → privater Fork), Status/Ergebnis anzeigen. Nutzt GitHub API (`merge-upstream` / Sync-fork-Flow); GH-PAT mit passenden Rechten; nur Admin. Entlastet manuelles `git fetch upstream && merge` bzw. GitHub-UI (vgl. `docs/PUBLIC-UPSTREAM.md`). |
| Erledigt | Betrieb / Recovery | Passwort-Reset über D1 (SQLite) | `npm run hash-password` / `scripts/hash-password.mjs` (lokal, Shared-Modul `shared/password-hash.mjs`); D1-`UPDATE` dokumentiert in `INSTALLATION.md`. Worker: Bootstrap, Login, User-Anlage nutzen dasselbe Modul. |
| Offen | DB / Deploy | Schema-Version + automatische Migrationen | **`schema_version`** (oder Meta-Tabelle) in D1; nummerierte Migrationsdateien (`migrations/001_….sql`, …). Beim **Build/Deploy** (oder `npm run db:migrate`): aktuelle Version lesen, fehlende Scripts der Reihe nach ausführen, Version hochzählen. Ersetzt manuelles SQL-Pflegen für dich und Kunden; `schema.sql` ggf. nur noch Baseline für frische Instanzen. Abstimmung mit bestehenden `db:migrate` / `db:migrate:remote` Scripts. |

**Status-Legende:** `Offen` · `In Arbeit` · `Erledigt`
